# 构建与部署由 GitHub Actions 完成（.github/workflows/deploy.yml）
# 此处仅声明发布目录与响应头，供手动拖拽部署时兜底
[build]
  publish = "site"
  command = ""

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
